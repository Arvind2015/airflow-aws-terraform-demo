name: Deploy

# Use case A: GitHub Actions runs Terraform.
#   - Pull request  -> terraform plan, posted as a PR comment (no changes applied)
#   - Merge to main -> terraform apply, then sync dags/ to the S3 bucket
#
# Region and bucket name are hardcoded in the Terraform config (terraform/) and
# below, so the only GitHub config needed is two repo Secrets:
#   AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
# (plus the Terraform state bucket named in terraform/backend.tf must exist).

on:
  pull_request:
    paths:
      - "terraform/**"
      - "dags/**"
      - ".github/workflows/deploy.yml"
  push:
    branches: [main]
    paths:
      - "terraform/**"
      - "dags/**"
      - ".github/workflows/deploy.yml"

permissions:
  contents: read
  pull-requests: write   # to post the plan comment

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

env:
  AWS_REGION: eu-west-1
  TF_IN_AUTOMATION: "true"

jobs:
  # ---------------------------------------------------------------------------
  plan:
    if: github.event_name == 'pull_request'
    name: terraform plan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: latest

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: fmt
        run: terraform fmt -check -recursive

      - name: init
        run: terraform init -input=false

      - name: validate
        run: terraform validate -no-color

      - name: plan
        id: plan
        run: terraform plan -input=false -no-color
        continue-on-error: true

      - name: Comment plan on PR
        uses: actions/github-script@v7
        with:
          script: |
            const body = `#### terraform plan — \`${{ steps.plan.outcome }}\`
            <details><summary>Show plan</summary>

            \`\`\`
            ${{ steps.plan.outputs.stdout }}
            \`\`\`
            </details>`;
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body,
            });

      - name: Fail if plan errored
        if: steps.plan.outcome == 'failure'
        run: exit 1

  # ---------------------------------------------------------------------------
  apply:
    if: github.event_name == 'push'
    name: terraform apply
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform
    outputs:
      bucket: ${{ steps.tf.outputs.bucket }}
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: latest
          terraform_wrapper: false   # so `terraform output` returns a clean value

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: init
        run: terraform init -input=false

      - name: apply
        run: terraform apply -input=false -auto-approve

      - name: Export outputs
        id: tf
        run: echo "bucket=$(terraform output -raw dags_bucket_name)" >> "$GITHUB_OUTPUT"

  # ---------------------------------------------------------------------------
  sync-dags:
    if: github.event_name == 'push'
    name: sync dags to S3
    runs-on: ubuntu-latest
    needs: apply   # bucket must exist first; also gives us its name
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync DAGs
        run: |
          aws s3 sync dags/ "s3://${{ needs.apply.outputs.bucket }}/dags/" \
            --delete \
            --exclude "*" \
            --include "*.py" \
            --no-progress
