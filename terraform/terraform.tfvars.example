# OPTIONAL. variables.tf now has working defaults (region eu-west-1,
# bucket prism-dags), so `terraform apply` works with no tfvars at all.
#
# Only copy this to terraform.tfvars if you need to override a default
# for a local run. terraform.tfvars is gitignored.

# aws_region       = "eu-west-1"
# dags_bucket_name = "prism-dags"
